{
  "name": "homebridge-nefit-easy2",
  "version": "1.0.1",
  "description": "Homebridge plugin for Nefit Easy thermostat using bosch-xmpp. Replaces the deprecated homebridge-nefit-easy on Node.js 22+.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "test -d dist || npm run build"
  },
  "keywords": [
    "homebridge-plugin",
    "nefit",
    "bosch",
    "thermostat",
    "homekit",
    "xmpp",
    "nefit-easy"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "homebridge": "^2.0.0",
    "node": "^22 || ^24"
  },
  "dependencies": {
    "bosch-xmpp": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "homebridge": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "homebridge": "^2.0.0"
  },
  "files": [
    "dist",
    "config.schema.json"
  ],
  "bundledDependencies": [
    "bosch-xmpp"
  ]
}
